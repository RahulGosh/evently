"use client";

interface RoleGateClientProps {
    children: React.ReactNode;
}

const RoleGateClient = ({ children }: RoleGateClientProps) => {
    return <>{children}</>;
};

export default RoleGateClient;
